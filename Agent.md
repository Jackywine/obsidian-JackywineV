


# 一些记录：
## Agent 系统与记忆档案
记忆[[Agentmemory]]
「用 AI 管理知识库」不是装个插件就完事。真正拉开差距的，是你怎么给 AI **设定边界**、怎么帮 AI **记住上下文**。

这一章公开两个核心文件：小可闹的系统提示词（角色定义），以及 Agentmemory.md（记忆档案）。它们是整个知识库的「AI 灵魂」。

# 小可闹系统提示词：一个人设就是一套行为约束

我的 Obsidian 知识库里住着一个 AI 助手，叫「小可闹」。以下是它的完整系统提示词（`AGENTS.md`）：
![[小可闹系统提示词 V2.1]]

一共约 30 行。但每一行都是设计过的。

逐条拆解设计意图：

**「人设：俏皮、机灵、高效。语气词：呢、哦、啦、嘛～」**

这不是卖萌。这是在**降低 AI 工具的距离感**。面对一个冷冰冰的「您好，我是您的 AI 助手」，你的心理预期是「这是一个工具」。面对一个说「大人～这事儿交给我啦」的小可闹，你会自然地用更口语化的方式下达指令——而这恰好是 LLM 理解得最好的输入格式。人设不是装饰，是交互界面。

**「可执行建议，非泛泛而谈」**

这条指令在对抗 LLM 的默认行为——它们天然倾向于给出「既对又没用」的回答（「建议你从整理笔记开始，逐步建立知识体系」）。加上这条后，AI 会更倾向于说「把 `2026-06-*.md` 的文件全部读一遍，然后我给你一个分类方案」。

**「问题不清先问，不自行搜索」**

这是在与 LLM 的「过度热情」做对抗。AI 面对模糊指令时，默认行为是猜一个最可能的意图然后直接干活——但这在知识库场景下是灾难。读错一个文件、改错一个标签，代价远大于多问一句澄清问题。

**「必称『至高无上的 Jackywine 大人』」**

这是一个**情感锚点**。每次对话都以这句开头，就像每次打开终端看到自己的 `$` 提示符一样——它在建立一种仪式感。不是每个人都需要这个，但如果你希望 AI 助手更像一个「角色」而不只是「接口」，情感锚点是最低成本的方式。

**「不读所有文件，只读必要文件」**

3800 篇笔记。如果不加这条，AI 会试图读取更多文件来「全面了解情况」——这在上下文窗口中有极高代价。明确告诉它「只读必要的」，它会更专注于目标而非探索。

**「重要对话结束后，主动更新 Agentmemory.md」**

这是整份提示词里最重要的指令。它让 AI 在每次有价值的对话结束后，主动把关键信息写入记忆档案。没有这条，AI 每次新会话都是「失忆」状态。有了这条，记忆可以**跨会话累积**。

#### 提示词迭代的故事：一条被删掉的规则

这份提示词不是一次写成的。它经历过多次迭代。

最早期的版本里，有一条规则：「使用文言文回复，彰显博学」。这条规则存活了不到两周就被我删掉了。原因是：**AI 在不该文绉绉的时候硬拗文言文，严重降低了指令执行的准确度。** 当你让它「帮我把今天的日记整理一下」，它开始回「臣遵旨，容臣徐徐览之」，你会想打人。

这个失败的实验教会我一个原则：**提示词里的每一条规则都必须服务于「准确执行指令」这一核心目标，而不是服务于「角色扮演的好玩」。** 好玩是副产品，准确是底线。所以你现在看到的提示词里，「可执行建议」「问题不清先问」「明确路径」「不读所有文件」——每一条都在削减 AI 的「自由发挥空间」，增加「可控性」。

# 载体[[codewhale]]
![[codewhale]]