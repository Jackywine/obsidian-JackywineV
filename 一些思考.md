# 哲学——文件、本地与开源的三位一体

工具配置讲完了。现在不聊「怎么用」，聊「为什么」。

为什么选 Obsidian 而不是 Notion？为什么执着于本地 Markdown 文件？为什么上下文管理是 AI 时代的核心元技能？这些问题回答清楚了，「怎么用」自然就通了。

### 4.1 为什么是 [[Obsidian]]？「本地优先」的三个承诺

Obsidian 官网上最醒目的标语不是「最强大的笔记工具」，而是——

Sharpen your thinking
打磨你的思考

这句标语背后是一套在 SaaS 时代堪称异类的设计哲学：

**承诺一：你的笔记以纯文本 Markdown 文件存储在本地设备上。**

即使 Obsidian 公司明天消失，你的数据依然完整可读。你可以用 VS Code、Vim、任何文本编辑器打开它们，可以用 grep、sed 等命令行工具处理它们，可以零转换成本迁移到任何兼容 [[Markdown]] 的工具。

**承诺二：核心功能完全免费。**

个人使用免费，商业使用也免费（2025 年 2 月起取消了商业许可证要求）。收费的只有增值服务：Sync 同步（$4/月）和 Publish 发布（$8/月）。这家公司从未接受外部融资，靠 7 个人的团队达到了 3.5 亿美元估值。

**承诺三：你的数据不属于任何服务器。**

没有强制云同步，没有服务器端渲染延迟，没有「你的文件在我们的数据中心」协议。隐私由物理控制保障——文件不离开你的硬盘。

这三个承诺之所以珍贵，是因为在 2026 年的工具生态中，它们是反主流的。Notion 的数据存在云端专有格式里，Logseq 虽然本地优先但与 Obsidian 的设计范式不同，Roam Research $15/月且数据不透明。真正把「数据属于用户」践行到极致的，Obsidian 是最彻底的一个。

对于我这样一个写了五年、积累了 3800 篇笔记的人来说，「数据持久性」不是口号——是真金白银的时间投资保障。

#### 知识库的数据画像：从混沌到秩序

7000 篇笔记放在一起是什么状态？说好听是「知识网络」，说真实就是「一片混沌」。我花了不少精力把混沌变成可导航的秩序，这里分享几个关键实践。
![[Snipaste_2026-07-06_10-39-44@2x.png]]
**MOC 体系：给知识一张地图。**

我不相信文件夹分类——一条笔记只属于一个文件夹，但可以属于多个主题。所以我的方案是「MOC」（Map of Content，内容地图）。

2026 年 6 月，我用 AI 把全部 7000 篇笔记分成了 10 个 MOC：[[MOC-总导航|MOC 总导航]] 作为入口，往下是 AI 与科技、设计与审美、知识管理与笔记、哲学与思想、学习与方法论、创作与内容、历史与人文、人物与传记、工具与技术、生活与日常。每个 MOC 里是关联笔记列表，底部有到其他 9 个 MOC 的交叉链接。

MOC 不是文件夹——它不会限制一条笔记只能属于一个分类。一条关于「AI 设计的伦理边界」的笔记可以同时出现在「AI 与科技」「设计与审美」「哲学与思想」三个 MOC 中。MOC 是建议性的导航，不是强制性的层级。

**数据看板：把知识库变成可视化报告。**

这是另一个有趣的实验。我让 AI 生成了一份知识库数据看板（[[Obsidian知识库数据看板-推特风格.html]]），暗色 Twitter 风格，ECharts 驱动。

看板里有：
- **笔记增长曲线**：从 2020 到 2026 年，每年新增笔记数量，可以看到 2024-2025 年是爆发期（AI 热潮期）
- **标签分布雷达图**：哪些标签密度最高（#AI、#设计、#知识管理 遥遥领先）
- **链接枢纽排行**：哪些笔记被链接次数最多（[[Obsidian]] 本身是链接之王，其次是 [[AI]]、[[设计]]）
- **孤岛笔记统计**：没有任何 wikilink 的笔记有多少——这些是待连接的候选

这个看板的价值不在于「好看」——在于它把隐藏在 7000 篇笔记里的**宏观模式**暴露了出来。你能一眼看到你的兴趣重心在哪、你的知识哪些是「孤岛」、你的链接网络哪些节点是「超级枢纽」。

**从熵增到熵减：维护一个持续生长的知识库。**

知识库天然趋向熵增——笔记越多越乱，越乱越不想整理，越不整理越乱。我反熵增的策略就一条：**用 AI 做 80% 的机械劳动，自己做 20% 的判断决策。**

举一个真实案例。某天我发现 Vault 从 891MB 膨胀到了不可接受的程度。我让 AI 分析占用空间——发现 103MB 是隐藏的缓存目录（.claudian/.codebuddy/.smart-env），235 张图片散落在各处，还有一堆临时脚本和 patch 文件。

AI 提出分类方案，我确认，AI 执行。10 分钟后 Vault 从 891MB 瘦到 788MB。我自己手动做这件事要花一下午——而且大概率会漏掉那些隐藏目录。

> 💡 维护一个知识库就像打理一个花园：不是「一次性修整」，而是「持续修剪」。每一周花 10 分钟让 AI 扫一遍——找孤岛笔记、找断链、找膨胀的文件——比你攒三个月再暴风雨式整理高效得多。

---

### 4.2 文件优于应用：从 Unix 哲学到 2026 的知识管理

「文件优于应用」听起来像一个技术教条。但追到根源，你会发现它来自半个世纪前的贝尔实验室。

我和我的 AI 助手小可闹有过一次对话。从 `.zshrc` 是什么开始，一路追问到 1971 年 Ken Thompson 写第一个 shell 的故事，最终抵达了 Unix 哲学的六条核心原则。那次对话总结成了一篇笔记：[[从 zshrc 到 Unix 哲学：一场追问的思维之旅|从 .zshrc 到 Unix 哲学]]。

Unix 哲学中，有五条直接映射到知识管理：

**1. 小而美——做好一件事，然后组合。**

`wc` 只数数，`grep` 只搜索，`sort` 只排序。四个小工具通过管道 `|` 串起来，组成一个天才。

在知识管理中：一篇笔记只讲一个观点，然后用 `[[wikilink]]` 连接。3000 篇原子笔记的组合，远胜过 100 篇万言书。

**2. 一切皆文本——文本是人类和机器都能读懂的通用接口。**

Unix 程序之间不靠复杂的 API 协议，就是纯文本流。一个程序输出文本，通过管道喂给下一个程序。

Markdown 是 2026 年的「纯文本通用接口」——人可读、AI 可解析、git 可版本控制。你知识库里的每一篇 `.md` 笔记，本质上都是 Unix 哲学在半个世纪后的延续。

**3. 抽象（壳）——最好的设计是给复杂包一层壳。**

Shell 的名字本意就是「壳」——把复杂的内核包起来。用户不需要知道系统调用的细节，只需要敲 `ls`。

在知识管理中：标签、MOC（内容地图）、索引——这些都是「壳」，帮你在 3000 篇笔记的复杂中找到简单入口。AI 助手（包括小可闹）本质上就是 Human ↔ Knowledge 之间的那一层 Shell。

**4. 懒人驱动进步——自动化一切重复劳动。**

Ken Thompson 写第一个 shell，纯粹是不想每次操作都写 C 代码再编译。

在知识管理中：AI 帮你自动打标签、自动建链接、自动生成 MOC、自动回顾遗忘的笔记。你的那一点「懒得手动整理」，恰恰是创造力释放的空间。

**5. 渐进式披露——简单的事简单，难的事可能。**

Shell 的设计：你只会 `ls` 也能用，会 `|` 组合就进阶一层，会写脚本就再进一步。从不逼你一开始就会所有东西。

在知识管理中：新手用 flomo 快速捕获想法，熟练后用 Obsidian 深度链接，进阶后用 AI 批量处理和自动化。每一层都有适合的工具，没有人需要第一天就会所有东西。

把 Unix 哲学放到知识管理的语境里，「文件优于应用」的深层含义是这样的：

> **你的知识不应该绑定在任何特定工具的专有格式里。工具可以换，数据不能丢。应用提供的是一个「视角」——一个暂时观察和操作数据的窗口——而数据本身应该以最通用、最持久、最可迁移的形式存在。**

`.md` 文件就是这个「最通用的形式」。

---

### 4.3 上下文对 Agent 到底意味着什么

这是一个我花了很长时间才真正理解的概念。

简单说：**上下文是 AI 的「工作记忆」。** 你发给 AI 的每条消息、AI 读取的每个文件、AI 运行的每个命令的结果——所有这些都占据着上下文窗口的空间。上下文窗口有上限，满了就必须「压缩」——总结重要信息、丢弃细节。

这意味着什么？

**意味着每次和 AI 对话，都在和遗忘赛跑。**

当上下文接近上限，AI 会自动压缩对话，但压缩意味着细节的损失。你前面花了 10 轮对话建立的共识、你在中间提的一两个「顺便说一下」的需求、AI 读了 5 个文件后记住的细节——这些都可能在压缩中被「总结」成一句话，甚至被丢弃。

这就是为什么**记忆档案（Agentmemory.md）如此重要**。它是上下文的「外置硬盘」——把最核心的信息持久化到文件中，这样即使上下文窗口被清空，下次对话时 AI 读一遍记忆档案就能恢复核心认知。

三个实战经验：

**第一，具体的 prompt > 模糊的 prompt。**

反直觉但成立。模糊的「帮我整理一下」会让 AI 先花大量 token 探索你的文件结构、试错、猜测你的意图。而具体的「帮我把 `flomo 随想/` 里今天的三条随想拆成独立笔记，放在 `02_知识卡片/` 下，用 `[[wikilink]]` 链接到相关的已有笔记」——虽然看似更「啰嗦」，但总 token 消耗更低，因为 AI 不需要试错。

**第二，关掉不用的 MCP 服务器。**

MCP 服务器默认会把所有可用工具加载到上下文中——即使你不用的也在占用空间。如果你配置了 5 个 MCP 但当前任务只用 1 个，剩下 4 个就是纯浪费。

**第三，子 Agent 隔离上下文。**

子 Agent 有自己独立的上下文窗口。大任务拆小，每个子 Agent 只带任务需要的上下文，完成后只返回结果摘要给主 Agent。这就像把一个大项目拆给多个只关注自己模块的工程师——每个人的「脑子」都很清爽。

而底层模型在让这一切更可行。我用的 DeepSeek V4 支持**百万 token 上下文**，通过混合注意力（CSA + HCA）将长上下文的 KV cache 压缩到仅需 V3.2 的 10%。这意味着同样的硬件可以跑更长的对话，同样的对话成本大幅降低。

> 💡 上下文管理是 AI 时代最被低估的元技能。大多数人只关心 prompt 怎么写得更好——但真正的高手在关心「怎么让 prompt 和上下文之间的关系更高效」。这和人类的工作记忆训练是同一个道理：不是记住更多，而是更精确地选择记住什么。


## 尾声：这个知识库是活的

写到这里，我意识到这份指南其实在讲一件事：

**个人知识管理在 AI 时代的范式转移。**

五年前，做知识管理 = 选一个好用的笔记软件 + 养成记笔记的习惯。

现在，做知识管理 = 选一个数据归属你的底座（Obsidian）+ 搭一套 AI 能理解的系统（提示词 + 记忆档案 + Skills + 插件）+ 让 AI 成为你的知识合伙人而非打字机。

这个转变的核心是：**你不再是唯一的劳动者。** 你的工作是「设定标准、做判断、做连接」，AI 的工作是「批量执行、发现模式、提醒遗忘」。

开放这个知识库，不是因为它已经完美。恰恰相反——它每天在生长，每天在试错。它不是一个「完成品」，而是一个「进行中的样本」。

**这个样本能告诉你的：**
- 一个真实的 Obsidian 重度用户的完整配置长什么样
- 一套完整的 Agent 系统（[[提示词]] + [[Agentmemory]] + [[Skills]]）怎么搭
- AI 从四个方向进入知识库的实践路径
- 「[[文件优于应用]]」「本地优先」「上下文管理」这些概念在实际操作中意味着什么

**这个样本不能替代的：**
- 你自己的思考和你自己的连接。知识库是外化的神经回路——AI 可以帮你布线，但突触长在哪里，只有你自己知道。

[[Jackywine 知识库欢迎你的到来]]